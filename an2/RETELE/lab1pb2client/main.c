//problema 1 doar ca cu udp
#include<stdio.h>
#include<string.h>
#include<stdlib.h>
#include<arpa/inet.h>
#include<sys/socket.h>
#include <unistd.h>

int main(void)
{
    int c;
    struct sockaddr_in server;

    c = socket(AF_INET, SOCK_DGRAM, 0);
    memset()
    return 0;
}
